# /mao-memory

Update durable Meta Agent OS memory.

## Instructions

1. Read:
   - `meta-agent-os/05_memory/project_brain.md`
   - `meta-agent-os/05_memory/decision_log.md`
   - `meta-agent-os/05_memory/assumptions_log.md`
   - `meta-agent-os/05_memory/changelog.md`
   - recent outputs under `meta-agent-os/03_outputs`

2. Identify durable facts, decisions, assumptions, and changes from the current work.

3. Update only the relevant memory files:
   - Put project context in `project_brain.md`.
   - Put decisions in `decision_log.md`.
   - Put assumptions in `assumptions_log.md`.
   - Put completed changes in `changelog.md`.

4. Preserve old context. Append dated updates instead of overwriting useful history.

5. End with the standard Meta Agent OS handoff.
